const authRouter = require("./auth");
const userRouter = require("./user");
const spotRouter = require("./spot");
const reviewRouter = require("./review");

module.exports = { authRouter, userRouter, spotRouter, reviewRouter };
